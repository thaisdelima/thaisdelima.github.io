import styled from 'styled-components';

const StyledTitle = styled.h1`
  font-size: 1.5rem;
`;

const StyledSubTitle = styled.h3`
  margin: 2rem;
`;

const StyledDiv = styled.div`
  margin: 10rem 2rem;
  display: flex;
  flex-direction: column;
`;

export { StyledTitle, StyledSubTitle, StyledDiv };
